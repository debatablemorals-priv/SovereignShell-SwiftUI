name: APIsec Scan

on:
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  apisec:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: APIsec intentionally disabled
        run: echo "APIsec workflow disabled until valid APIsec project/configuration is available."
