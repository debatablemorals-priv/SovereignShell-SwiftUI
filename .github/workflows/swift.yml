name: Swift Build and Smoke Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

concurrency:
  group: swift-smoke-${{ github.ref }}
  cancel-in-progress: true

jobs:
  smoke:
    runs-on: macos-14
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Swift version
        run: swift --version

      - name: Build app target
        run: swift build --target SovereignShell-SwiftUI --verbose

      - name: Build tests
        run: swift build --build-tests --verbose

      - name: Run curated smoke tests
        run: swift test --filter CISmokeTests --verbose